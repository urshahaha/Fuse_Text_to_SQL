# Text-to-SQL Agent System

This project implements a Text-to-SQL system using FastAPI, PostgreSQL, and Python. 
The system takes natural language questions, converts them into SQL queries, executes them on the database, and returns the result with a simple natural language summary.
